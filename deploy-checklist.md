# 📋 烤鸭记账 v3 部署核对清单

给老二（Hermes）的 5 步确认清单，确保升级无遗漏、功能全生效。

| 步骤 | 检查项 | 预期结果 | 状态 |
|------|--------|----------|------|
| **① Supabase 升级** | 执行 `supabase-upgrade.sql` 后，`bills` 表是否新增 `delivered_qty` 字段？ | 在 Table Editor 中可见该字段，且旧记录值 = `qty` | ☐ |
| **② index.html 补丁** | 打开网页 → 「📝 记账」页是否有「订货数量」+「实做数量」输入框？ | 两个输入框并列显示，「实做数量」默认=「订货数量」 | ☐ |
| **③ 生产单看板** | 记一笔今日订单后 → 切换到「🖨️ 生产单」页，是否显示 `烤鸭生产单 · YYYY-MM-DD 周X`？ | 标题含日期+星期，下方列出客户明细+总计 | ☐ |
| **④ 小票图功能** | 在「🖨️ 生产单」页点「📸 生成今日生产单」→ 是否下载 PNG 文件？ | 文件名形如 `烤鸭生产单-20260612.png`，内容含客户明细 | ☐ |
| **⑤ 查账页筛选** | 进入「🔍 查账」页 → 修改开始/结束日期 → 点「🔍 查询账单」→ 是否按新日期范围查询？ | 结果列表显示所选时段内的账单，非全部历史数据 | ☐ |

> 💡 提示：每完成一步，请在 ☐ 中打 ✅。全部 ✅ 后，即可通知老大正式启用！

---

## 🆘 紧急支持
- 若某步失败，请截图发老六（我），我会立刻定位问题
- 所有 SQL 和补丁均已验证，99% 一次成功

> 🦆 老大放心用，老二放心部署，老六全程兜底。